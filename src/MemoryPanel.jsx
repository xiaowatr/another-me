import React,{useState} from 'react';import {sameMemory} from './memory-review';
const names={reality:'现实信息',preference:'态度与偏好',fiction:'角色经历或计划'};
export default function MemoryPanel({memories,candidates,onCandidates,onMemories,onSaved=()=>{},disabled}){const [editing,setEditing]=useState(null),[text,setText]=useState(''),[error,setError]=useState('');const source=m=><details><summary>查看来源</summary><p>{m.sourceText||'此前保存的内容'}</p></details>;return <section className="memory-panel">{error&&<p role="alert">{error}</p>}{candidates.length>0&&<section><h3>待保存内容</h3>{candidates.map(c=><article className="memory-item" key={c.id}><label>{names[c.type]||'此前留下的内容'}<textarea disabled={disabled} value={c.text} onChange={e=>onCandidates(candidates.map(x=>x.id===c.id?{...x,text:e.target.value}:x))}/></label>{!c.type&&<label>保存为<select value={c.type||''} onChange={e=>onCandidates(candidates.map(x=>x.id===c.id?{...x,type:e.target.value}:x))}><option value="">请选择</option>{c.sourceRole!=='assistant'&&<><option value="reality">现实信息</option><option value="preference">态度与偏好</option></>}<option value="fiction">角色经历或计划</option></select></label>}{source(c)}<button disabled={disabled} className="text-button" onClick={()=>onCandidates(candidates.filter(x=>x.id!==c.id))}>删除</button></article>)}<button className="text-button" disabled={disabled||candidates.some(c=>!c.text.trim()||!c.type)} onClick={()=>{let next=[...memories];for(const c of candidates){if(c.sourceRole==='assistant'&&c.type!=='fiction')continue;next=next.filter(m=>m.id!==c.id&&m.id!==c.replacesId);if(!next.some(m=>sameMemory(m.text,c.text)))next.push({...c,savedAt:new Date().toISOString()});}if(next.length>30){setError('记忆已满，请先删去不再需要的内容。');return;}setError('');onMemories(next);onCandidates([]);onSaved();}}>保存这些内容</button></section>}{['user','fiction'].map(group=><section key={group}><h3>{group==='user'?'他记得的我':'另一个自己的经历与计划'}</h3>{memories.filter(m=>group==='user'?m.type!=='fiction':m.type==='fiction').map(m=><article className="memory-item" key={m.id}><span className="tag">{names[m.type]}</span>{editing===m.id?<><label>修改记忆<textarea value={text} maxLength={1000} onChange={e=>setText(e.target.value)}/></label><button className="text-button" disabled={disabled||!text.trim()} onClick={()=>{onMemories(memories.map(x=>x.id===m.id?{...x,text:text.trim()}:x));setEditing(null);onSaved();}}>保存</button><button className="text-button" onClick={()=>setEditing(null)}>取消</button></>:<><p>{m.text}</p><button className="text-button" disabled={disabled} onClick={()=>{setEditing(m.id);setText(m.text);}}>编辑</button></>}{source(m)}<button className="text-button" disabled={disabled} onClick={()=>onMemories(memories.filter(x=>x.id!==m.id))}>删除</button></article>)}</section>)}{!memories.length&&<p className="small">聊完点击“先聊到这儿吧”，会整理值得留下的内容。</p>}</section>;}
