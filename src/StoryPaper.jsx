import {shouldOpenLetter,markLetterOpened} from './letter-state';
import React,{useEffect,useState} from 'react';
function firstVisit(id){try{return shouldOpenLetter(sessionStorage,id,window.matchMedia('(prefers-reduced-motion: reduce)').matches);}catch{return false;}}
export default function StoryPaper({id,children}){const [playing,setPlaying]=useState(()=>firstVisit(id));useEffect(()=>{try{markLetterOpened(sessionStorage,id);}catch{}const media=window.matchMedia('(prefers-reduced-motion: reduce)');const stop=()=>setPlaying(false);media.addEventListener('change',stop);const timer=setTimeout(stop,820);return()=>{clearTimeout(timer);media.removeEventListener('change',stop);};},[id]);return <StoryPaperFrame playing={playing} onSkip={()=>setPlaying(false)}>{children}</StoryPaperFrame>; }
export function StoryPaperFrame({playing,onSkip,children}){return <section className={'story-paper'+(playing?' unfolding':'')} aria-label="故事信纸">{playing&&<><button className="skip-letter text-button" onClick={onSkip}>跳过展开</button><div className="opening-envelope" aria-hidden="true"><div className="drawn-sheet"/><div className="envelope-pocket"/><div className="envelope-flap"/></div></>}<div className="paper-content">{children}</div></section>;}
