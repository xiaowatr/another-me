import React,{useEffect,useRef} from 'react';
export default function AboutMe({open,onClose,children,title="关于我"}){const dialog=useRef(null);useEffect(()=>{if(!open)return;const before=document.activeElement;dialog.current?.showModal();return()=>before?.focus?.();},[open]);if(!open)return null;return <dialog className="about-dialog" ref={dialog} aria-label={title} onCancel={e=>{e.preventDefault();onClose();}}><div className="about-heading"><h2>{title}</h2><button type="button" className="text-button" onClick={onClose} aria-label={"关闭"+title}>关闭</button></div>{children}<details className="settings"><summary>设置</summary><p className="small">仅保存在此浏览器，清除数据后会丢失。</p></details></dialog>;}
