import test from 'node:test';import assert from 'node:assert/strict';import {watchChatViewport} from '../src/chat-viewport.js';
test('document coordinates follow viewport without reading displaced shell; history not forced on scrolling',()=>{const events=new Map(),props=new Map(),body={style:{}},html={style:{}},media={matches:true,addEventListener(){},removeEventListener(){}};let n=0;const win={document:{body,documentElement:html,querySelector:()=>null},scrollY:0,innerHeight:844,visualViewport:{height:400,offsetTop:100,pageTop:160,addEventListener(){},removeEventListener(){}},matchMedia:()=>media,requestAnimationFrame:f=>{f();return 1},cancelAnimationFrame(){},addEventListener:(k,f)=>events.set(k,f),removeEventListener:k=>events.delete(k),scrollTo(){}};const stop=watchChatViewport(win,{style:{setProperty:(k,v)=>props.set(k,v),removeProperty:k=>props.delete(k)}},()=>n++);for(let i=0;i<20;i++)events.get('scroll')();assert.equal(props.get('--chat-offset'),'160px');assert.equal(body.style.position,'static');assert.equal(n,1);win.visualViewport.pageTop=0;win.visualViewport.height=844;events.get('resize')();assert.equal(props.get('--chat-offset'),'0px');stop();assert.equal(props.size,0);});
